description: Telemetry agent — cycles awkward JSON log shapes, OTLP/HTTP, and W3C traces for the Logs and Trace screens
command:
  - python3
  - main.py
working_dir: telemetry
health:
  type: process
extends: python-http
