name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  setup:
    name: Setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - name: Install
        working-directory: app
        run: bun install --frozen-lockfile

  tests:
    name: Tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - working-directory: app
        run: bun install --frozen-lockfile
      - name: bun test
        working-directory: app
        run: bun test

  typecheck:
    name: Typecheck
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - working-directory: app
        run: bun install --frozen-lockfile
      - name: tsc --noEmit
        working-directory: app
        run: bunx tsc --noEmit

  demo-config:
    name: Demo config
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - working-directory: app
        run: bun install --frozen-lockfile
      - name: config validate
        working-directory: examples/demo-platform
        run: bun run ../../app/src/bin.ts config validate

  compile-smoke:
    name: Compile smoke
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - working-directory: app
        run: bun install --frozen-lockfile
      - name: bun build --compile linux-x64
        working-directory: app
        run: bun build --compile --target=bun-linux-x64 --outfile /tmp/devctl-linux-x64 src/bin.ts

  windows-tests:
    name: Windows tests
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - working-directory: app
        run: bun install --frozen-lockfile
      - name: bun test
        working-directory: app
        run: bun test

  audit:
    name: Audit
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - working-directory: app
        run: bun install --frozen-lockfile
      - name: bun audit
        working-directory: app
        run: bun audit

  codeql:
    name: CodeQL
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      security-events: write
    steps:
      - uses: actions/checkout@v7
      - uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript
          source-root: app
      - uses: github/codeql-action/analyze@v4

  dependency-review:
    name: Dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/dependency-review-action@v5
        with:
          fail-on-severity: high

  ci:
    name: CI
    needs: [tests, typecheck, demo-config, compile-smoke, windows-tests, audit, codeql, dependency-review]
    if: always() && !cancelled()
    runs-on: ubuntu-latest
    steps:
      - name: Require every check
        env:
          TESTS: ${{ needs.tests.result }}
          TYPECHECK: ${{ needs.typecheck.result }}
          DEMO: ${{ needs.demo-config.result }}
          COMPILE: ${{ needs.compile-smoke.result }}
          WINDOWS: ${{ needs.windows-tests.result }}
          AUDIT: ${{ needs.audit.result }}
          CODEQL: ${{ needs.codeql.result }}
          DEPS: ${{ needs.dependency-review.result }}
        run: |
          test "$TESTS" = success
          test "$TYPECHECK" = success
          test "$DEMO" = success
          test "$COMPILE" = success
          test "$WINDOWS" = success
          test "$AUDIT" = success
          test "$CODEQL" = success
          test "$DEPS" = success -o "$DEPS" = skipped
