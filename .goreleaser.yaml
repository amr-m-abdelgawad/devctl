version: 2
builds:
  - id: devctl
    main: ./cmd/devctl
    binary: devctl
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0
archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
changelog:
  sort: asc
